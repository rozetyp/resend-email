# Copy to .env and set your own values (do NOT commit .env)
RESEND_API_KEY=re_XXXXXXXXXXXXXXXXXXXXXXXX
DEFAULT_FROM="Your Name <no-reply@yourdomain.com>"
# Optional: protect the UI with basic auth. For local testing leave REQUIRE_AUTH=0
# To enable auth set REQUIRE_AUTH=1 and provide ADMIN_USER/ADMIN_PASS
REQUIRE_AUTH=0
ADMIN_USER=admin
ADMIN_PASS=changeme
